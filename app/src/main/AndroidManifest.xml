<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="jp.codepanic.reversiclassic"
    android:installLocation="preferExternal"
    android:versionCode="15"
    android:versionName="2.4" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="18" />
    
    <!-- タブレット対応：targetは14以上で、minが11未満なら以下が必要 -->
    <supports-screens
        android:anyDensity="true"
        android:smallScreens="true"
        android:normalScreens="true"
        android:largeScreens="true"
        android:xlargeScreens="true" 
        />
    
    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@android:style/Theme.NoTitleBar" >
        <activity
            android:name="jp.codepanic.reversiclassic.MainActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name="jp.codepanic.reversiclassic.ConfigActivity"
            android:screenOrientation="portrait">
            
        </activity>
<!-- 
	    <receiver android:name="SampleReceiver">
	        <intent-filter>
	            <action android:name="jp.codepanic.reversiclassic.pass"/>
	        </intent-filter>
	    </receiver>
-->

    </application>

    <meta-data android:name="Adlantis_Publisher_ID" android:value="Mjg5OTk%3D%0A" />
</manifest>
